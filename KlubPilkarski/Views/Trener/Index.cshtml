@model IEnumerable<KlubPilkarski.Models.Trener>

@{
    ViewBag.Title = "Lista trenerów";
}

<h2>Lista trenerów</h2>

<p align="right">
    <button type="button" class="btn btn-primary add-button"
            onclick="location.href='@Url.Action("Create", "Trener")'"
            data-toggle="tooltip" data-placement="top" title="Dodaj trenera">
        <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Dodaj trenera
    </button>
</p>
<table id="table" class="table">
    <thead>
        <tr>
            <th>
                Imię
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwisko)
            </th>
            <th></th>
        </tr>
    </thead>
    <tfoot>
        <tr>
            <th>
                Imię
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nazwisko)
            </th>
            <th></th>
        </tr>
    </tfoot>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Imie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nazwisko)
                </td>
                <td>
                    <button type="button" class="btn btn-warning btn-sm edit-button"
                            onclick="location.href='@Url.Action("Edit", "Trener", new { id = item.IdT })'"
                            data-toggle="tooltip" data-placement="top" title="Edytuj trenera">
                        <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                    </button>
                    <button type="button" class="btn btn-primary btn-sm"
                            onclick="location.href='@Url.Action("Details", "Trener", new { id = item.IdT })'"
                            data-toggle="tooltip" data-placement="top" title="Pokaż szczegółowe informacje">
                        <span class="glyphicon glyphicon-file" aria-hidden="true"></span>
                    </button>
                    <button type="button" class="btn btn-danger btn-sm delete-button"
                            onclick="location.href='@Url.Action("Delete", "Trener", new { id = item.IdT })'"
                            data-toggle="tooltip" data-placement="top" title="Usuń trenera">
                        <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
                    </button>
                </td>
            </tr>
        }
    </tbody>

</table>
